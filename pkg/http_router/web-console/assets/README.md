# Embedded web console archive

- `placeholder.zip` is an empty zip used for local builds without a packed UI.
- `web_console.zip` is generated by `go run ./scripts/cmd/pack_web_console` and is gitignored.
- Release / container builds compile with `-tags=embedui` after packing so the server binary contains the UI.
