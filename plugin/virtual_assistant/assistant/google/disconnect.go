package google_assistant

import (
	gaTY "github.com/mycontroller-org/server/v2/plugin/virtual_assistant/assistant/google/types"
)

func (a *Assistant) runDisconnectRequest(request gaTY.Request) {
	// a.logger.Info("*** received disconnect request ***", zap.Any("request", request))
}
